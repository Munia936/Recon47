version: "3.9"
services:
  recon47:
    build: .
    image: recon47:2.0
    volumes:
      - ./reports:/reports
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
    # Usage: docker-compose run recon47 -t example.com --all -o /reports/scan
